# 功能菜单说明

## 功能入口

所有功能都可以通过首页（home页面）的功能菜单访问。

## 功能分类

### 1. 音频处理功能

#### 🎵 音频降噪
- **路径**：`pages/DeNoise/DeNoise`
- **功能**：基于巴特沃斯滤波器算法进行音频降噪处理
- **特点**：
  - 支持多种音频格式（WAV、MP3、OGG等）
  - 降噪前后音频对比试听
  - 降噪后文件下载
- **访问方式**：首页菜单 → 音频降噪，或底部TabBar

#### 🎼 音频分离
- **路径**：`pages/AudioSep/AudioSep`
- **功能**：将音频分离成人声、伴奏、鼓声、贝斯等
- **特点**：
  - 支持多种音频格式
  - 分离后各音轨独立播放
  - 支持单独下载各音轨
- **访问方式**：首页菜单 → 音频分离

#### 🎹 分离音频
- **路径**：`pages/separateAudio/separateAudio`
- **功能**：音频分离的另一种实现方式
- **访问方式**：首页菜单 → 分离音频

#### 🎤 录音功能
- **路径**：`pages/soundRecord/soundRecord`
- **功能**：实时录音功能
- **特点**：
  - 支持实时录音
  - 录音文件保存
  - 录音播放
- **访问方式**：首页菜单 → 录音功能

### 2. AI聊天功能

#### 💬 ChatGPT
- **路径**：`pages/chatGPT/chatGPT`
- **功能**：基于ChatGPT的智能聊天
- **特点**：
  - 实时对话
  - 语音转文字
  - 聊天记录保存
- **访问方式**：首页菜单 → ChatGPT

#### 🤖 GPT聊天
- **路径**：`pages/GPT/GPT`
- **功能**：GPT模型聊天功能
- **访问方式**：首页菜单 → GPT聊天

#### 📝 创建对话
- **路径**：`pages/CreateDialo/createDialo`
- **功能**：创建和管理对话记录
- **访问方式**：首页菜单 → 创建对话

#### ⚙️ 模型参数
- **路径**：`pages/EditCsb/EditCsb`
- **功能**：编辑聊天模型的参数配置
- **访问方式**：首页菜单 → 模型参数

### 3. 其他功能

#### 📞 电话功能
- **路径**：`pages/telphone/telphone`
- **功能**：电话相关功能
- **访问方式**：首页菜单 → 电话功能

#### 🔊 音频登录
- **路径**：`pages/LoginAudio/LoginAudio`
- **功能**：通过音频进行登录验证
- **访问方式**：首页菜单 → 音频登录

#### 📢 音频注册
- **路径**：`pages/registerAudio/registerAudio`
- **功能**：通过音频进行注册
- **访问方式**：首页菜单 → 音频注册

#### 📋 历史记录
- **路径**：`pages/history/history`
- **功能**：查看音频处理历史记录
- **特点**：
  - 降噪历史记录
  - 分离历史记录
  - 播放历史文件
  - 下载历史文件
- **访问方式**：首页菜单 → 历史记录，或底部TabBar

## 页面导航说明

### TabBar页面（底部导航栏）
以下页面可以通过底部TabBar直接访问：
- 音频降噪（DeNoise）
- 历史记录（history）
- 数据分析（DtLine）
- 用户管理（chat）
- 我的（home）

### 普通页面（通过菜单访问）
以下页面需要通过首页菜单或页面跳转访问：
- 音频分离（AudioSep）
- 分离音频（separateAudio）
- ChatGPT（chatGPT）
- GPT聊天（GPT）
- 创建对话（CreateDialo）
- 模型参数（EditCsb）
- 电话功能（telphone）
- 音频登录（LoginAudio）
- 音频注册（registerAudio）
- 录音功能（soundRecord）

## 功能菜单布局

首页功能菜单采用4列网格布局，包含12个功能入口：

```
┌─────────┬─────────┬─────────┬─────────┐
│ 音频降噪 │ 音频分离 │ 分离音频 │ 录音功能 │
├─────────┼─────────┼─────────┼─────────┤
│ ChatGPT │ GPT聊天 │ 创建对话 │ 模型参数 │
├─────────┼─────────┼─────────┼─────────┤
│ 电话功能 │ 音频登录 │ 音频注册 │ 历史记录 │
└─────────┴─────────┴─────────┴─────────┘
```

## 使用说明

1. **访问功能**：打开小程序后，默认进入首页（我的），点击功能菜单中的任意图标即可进入对应功能页面。

2. **返回首页**：在功能页面中，可以通过以下方式返回：
   - 点击底部TabBar的"我的"按钮
   - 使用小程序自带的返回按钮

3. **快速访问**：常用功能（音频降噪、历史记录）可以通过底部TabBar快速访问。

## 注意事项

1. 部分功能需要登录后才能使用
2. 音频处理功能需要上传音频文件，建议在WiFi环境下使用
3. AI聊天功能需要网络连接
4. 录音功能需要授权麦克风权限

