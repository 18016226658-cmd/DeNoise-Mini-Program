# 系统启动和使用指南

## 一、启动后端服务

### 方法1: 使用 app.py（推荐）
```bash
cd backend
python app.py
```

### 方法2: 使用启动脚本
```bash
cd backend
python run_server.py
```

### 验证后端启动成功
看到以下输出表示启动成功：
```
[成功] 音频路由模块已注册
[成功] 用户路由模块已注册
[成功] 用户路由模块（routes.user）已注册
[成功] AI路由模块已注册
[成功] 数据路由模块已注册
[成功] 管理路由模块已注册
[成功] 统计路由模块已注册
 * Running on http://127.0.0.1:5000
```

## 二、启动前端

1. 打开微信开发者工具
2. 导入项目：选择 `wxminpro` 目录
3. 编译项目
4. 在模拟器中测试

## 三、测试核心功能

### 1. 音频降噪功能

**步骤**:
1. 登录系统（使用管理员账号：18016226658 / zy6658）
2. 点击底部导航栏的"音频降噪"
3. 点击"选择音频文件"
4. 选择音频文件（支持 wav, mp3, ogg, aac, flac, m4a）
5. 等待文件上传完成
6. 点击"开始降噪"按钮
7. 等待处理完成（几秒到几十秒）
8. 可以试听降噪前后的对比
9. 点击"下载"按钮下载降噪后的文件

**预期结果**:
- 文件成功上传到服务器
- 降噪处理成功完成
- 可以播放降噪后的音频
- 可以下载降噪后的文件

### 2. 音频分离功能

**步骤**:
1. 从首页点击"音频分离"菜单
2. 点击"选择音频文件"
3. 选择音频文件
4. 等待文件上传完成
5. 点击"开始分离"按钮
6. 等待处理完成（可能需要几分钟）
7. 可以试听分离后的各个音轨（人声、伴奏、鼓、贝斯）
8. 可以下载各个音轨文件

**预期结果**:
- 文件成功上传到服务器
- 分离处理成功完成
- 生成4个音轨文件（人声、其他、鼓、贝斯）
- 可以播放各个音轨
- 可以下载各个音轨文件

## 四、常见问题排查

### 问题1: ERR_CONNECTION_REFUSED
**原因**: 后端服务未启动
**解决**: 启动后端服务（见"一、启动后端服务"）

### 问题2: 上传失败
**检查**:
- 后端服务是否运行
- 文件格式是否支持
- 文件大小是否过大（建议 < 50MB）

### 问题3: 降噪/分离失败
**检查**:
- 后端日志查看错误信息
- 确保音频文件格式正确
- 检查磁盘空间是否充足
- 对于分离功能，确保已安装 demucs: `pip install demucs`

### 问题4: 文件找不到
**检查**:
- 确保文件已成功上传到 `backend/Audio/input` 目录
- 检查文件名是否正确

## 五、功能验证清单

- [ ] 后端服务成功启动
- [ ] 前端可以连接后端（无 ERR_CONNECTION_REFUSED）
- [ ] 可以上传音频文件
- [ ] 降噪功能正常工作
- [ ] 分离功能正常工作（如果安装了demucs）
- [ ] 可以播放处理后的音频
- [ ] 可以下载处理后的文件
- [ ] 用户信息页面正常显示
- [ ] 管理员可以查看可视化界面

## 六、系统要求

### 后端要求
- Python 3.7+
- Flask
- PyMySQL
- pydub
- mutagen
- demucs (用于音频分离，可选)

### 前端要求
- 微信开发者工具
- 小程序基础库 2.0+

### 数据库要求
- MySQL 5.7+
- 数据库名: audio
- 必要的表: users, denoisetable, sepaudiotable


